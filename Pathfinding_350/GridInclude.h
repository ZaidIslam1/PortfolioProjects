#pragma once

#include <algorithm> // For std::reverse (to reverse the path vector)
#include <cassert>   // For std::cassert
#include <cmath>     // For std::sqrt (heuristic calculation)
#include <map>
#include <queue> // For std::priority_queue (A* algorithm)
#include <set>   // For std::unordered_set (tracking visited nodes)
#include <tuple> // For std::tuple (to define Node in A*)
#include <unordered_map>
#include <vector> // For std::vector (storing the map and paths)